
import config from "../config";

// API URLs
const mainPage = '/mainpage';
const homePath = '/';
const UserInfo = '/userinfo';

// Substrings
const ApiPath = config.apiUrl

const apiPrefix = '/api/'

// Asset URLs

export default {
    mainPage: mainPage,
    homePath: homePath,
    UserInfo: UserInfo,
    apiPrefix: apiPrefix,
    ApiPath: ApiPath,

 
}
